diff --git a/app/api/rh/join/base/route.ts b/app/api/rh/join/base/route.ts
index 0000000..0000000 100644
--- a/app/api/rh/join/base/route.ts
+++ b/app/api/rh/join/base/route.ts
@@ -35,11 +35,7 @@ export async function POST(request: NextRequest) {
         console.log(`[v0] API: Processing file ${i + 1}/${files.length}:`, file.name)
         try {
-          // Convert base64 back to binary
-          const binaryString = atob(file.data)
-          const bytes = new Uint8Array(binaryString.length)
-          for (let j = 0; j < binaryString.length; j++) {
-            bytes[j] = binaryString.charCodeAt(j)
-          }
+          // Convert base64 back to binary (Node runtime safe)
+          const bytes = Buffer.from(file.data, 'base64')